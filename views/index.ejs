<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>All Posts</title>
</head>

<body>
    <div class="quoraBody">
        <h1>Quora Posts</h1>
        <% for(let post of posts){ %>
            <div class="post">
                <h3 class="user">@<%=post.username%>
                </h3>
                <h4 class="content">
                    <%=post.content%>
                </h4>
                <a href="http://localhost:8080/posts/<%= post.id %>">See in detail</a>
                <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
                <form action="/posts/<%= post.id %>?_method=DELETE" method="post">
                    <button>Delete Post</button>
                </form>
            </div>
            <% } %>
                <a class="createPost" href="http://localhost:8080/posts/new">Create New Post</a>
    </div>
</body>

</html>
